export default function App() {
  return (
    <main className="shell">
      <section className="hero">
        <p className="eyebrow">MoneyLeap</p>
        <h1>React is ready.</h1>
        <p className="lede">
          This repository now has a clean Vite + React + TypeScript starter you can build on.
        </p>
        <div className="card">
          <span className="card-label">Next step</span>
          <strong>Run `npm install` and `npm run dev`.</strong>
        </div>
      </section>
    </main>
  );
}
